# Render Blueprint configuration
services:
  - type: web
    name: vector-deploy-create-movie-booking-app-c14ba6dd
    env: node
    plan: starter
    buildCommand: npm install && npm run build
    startCommand: npm start
    envVars:
      - key: NODE_ENV
        value: production
